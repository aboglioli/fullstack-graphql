import React from 'react';
import { Link } from 'react-router-dom';
import './Nav.css';

const Nav = ({ ...props }) => {
  return (
    <nav className="nav" {...props}>
      Asd
    </nav>
  );
};

export default Nav;
